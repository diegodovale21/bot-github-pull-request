{
  "name": "github-pr-bot-scripts",
  "version": "1.0.0",
  "description": "Scripts para geração automática de descrição de PRs",
  "main": "generate-pr-body.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "@octokit/core": "^7.0.6",
    "openai": "^4.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
